spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  profiles:
    active: native
  application:
    name: config-server
server:
  port: 8770
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8761/eureka/
