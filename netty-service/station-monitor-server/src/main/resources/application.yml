spring:
  application:
    name: station-monitor-server
netty:
  port: 7777
server:
  port: 8765
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
feign:
  hystrix:
    enabled: true #熔断器hystrix

